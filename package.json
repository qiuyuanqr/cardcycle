{
  "name": "cardcycle",
  "version": "2.0.0",
  "description": "信用卡账单周期记录与还款提醒（核心逻辑 core.js 供网页/小程序/测试共用）",
  "private": true,
  "scripts": {
    "test": "node --test test/core.test.js && npm run check-sync",
    "sync-core": "cp core.js miniprogram/utils/core.js",
    "check-sync": "diff -q core.js miniprogram/utils/core.js"
  }
}
