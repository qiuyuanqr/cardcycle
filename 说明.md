# 卡周期 · 设计说明

一个管理信用卡账单周期的记录与提醒工具。用来解决：多张卡各有各的账单日，
每张卡都要在账单日之后刷、下个账单日之前还清，而人脑记不住十几条错开的循环。

打开方式：双击 `index.html`。手机上把这个文件存到「文件」App 或发到微信，
用浏览器打开即可。不需要联网、不需要注册、不需要装任何东西。

---

## 一、为什么不用表格

表格能记账，但记不了「时间」。这件事的难点不在记录金额，而在于：

- 每张卡的账单日不同，形成各自独立的 30 天循环，且循环在不断滚动
- 今天这张卡处于周期第几天、还剩几天必须还清，每天都在变
- 表格不会主动找你，你必须记得去打开它——而漏还一次，这个月就白做了

所以拆成两件事，各交给擅长的工具：

| 需求 | 工具 | 理由 |
|---|---|---|
| 记录、计算、查看状态 | 本地网页 | 打开就能看到所有卡的实时状态，四次点击记一笔 |
| 到点提醒 | 手机系统日历 | 不用开电脑、不用开网页，锁屏也会响，最可靠 |

网页负责算，算完一键导出 `.ics` 灌进手机日历。日历只管按时响。

---

## 二、时间模型

整个工具建立在一个计算上：**给定一张卡的账单日，今天处在周期的什么位置。**

```
        上个账单日            今天              还款截止        下个账单日
   ─────────┬─────────────────┼─────────────────┬───────────────┬────────→
            │                                   │               │
            └─ 次日起：安全刷卡窗口开始           └─ 缓冲 3 天 ──┘
```

三个关键日期：

- **安全刷卡窗口起点** = 上个账单日 + 1 天
  账单日当天刷的钱，多数银行会计入当期账单。所以账单日当天不要刷，等次日。

- **还款截止日** = 下个账单日 − 缓冲天数（默认 3 天）
  为什么要留缓冲：跨行还款可能次日才到账；银行是在账单日当天日终做结算，
  卡着账单日当天还，到账时间稍有延迟就前功尽弃。3 天是比较稳的余量。
  这个天数可以在「设置」里改。

- **周期归属**：每笔刷卡按其日期自动归入对应的账单周期，不用手工分类。
  月末账单日（29/30/31 号）在短月会自动落到当月最后一天。

### 五种状态

概览页每张卡显示一种状态，最紧急的自动排在最前面：

| 状态 | 含义 | 颜色 |
|---|---|---|
| 已出账单 | 有交易跨过了账单日仍未还，这部分已经上了账单 | 红 |
| 已超建议期限 | 过了还款截止日但还没到账单日，还来得及但要立刻还 | 红 |
| 今明还清 | 距还款截止 ≤ 1 天 | 橙 |
| 准备还款 | 距还款截止 2–4 天 | 黄 |
| 周期进行中 | 本期已刷、时间还宽裕 | 绿 |
| 本期未刷 | 本周期还没有刷卡记录 | 灰 |

---

## 三、四台机器怎么协调

机器是四个人共用的资源，所以必须集中在一处看，不能各记各的。
「机器」页有两个视图：

1. **按闲置时间排序** — 哪台机器最久没用过，一眼看到，排最上面
2. **卡 × 机器矩阵** — 每张卡在每台机器上「上次是几天前用的」

矩阵是核心。它回答的是「这张卡这次该用哪台机」这个每次都要做的决定。
绿色表示间隔已超过你设定的天数（默认 7 天，可改），橙色表示间隔还不够。

记一笔的时候，机器列表会**按闲置时间自动排序并默认选中最久没用的那台**，
每台机器下方标着「本卡 X 天前用过」。所以正常情况下不用思考，
打开弹窗默认选项就是合理的。

---

## 四、日常怎么用

**每天**：打开概览扫一眼。没有红黄色就什么都不用做。

**刷卡后**：点底部「＋ 记一笔刷卡」→ 选卡 → 选机器（默认已选好）→ 填金额 → 保存。

**还款后**：在卡片上点「登记还款」→「全部标记已还」。
如果只还了一部分，就在列表里逐笔点「已还」。

**每年一次**（或加了新卡之后）：设置 → 导出日历提醒，重新导入手机日历。

### 导入日历的操作

- **iPhone**：把 `.ics` 发到微信或邮箱 → 点开 → 选「加入日历」
- **Mac**：双击文件，日历 App 会问导入到哪个日历

建议**新建一个单独的日历分组**（比如叫「卡周期」）专门放这些提醒。
好处是下次重新导出前，可以整组删掉旧的，不会和旧提醒重复。

每张卡每月生成两条提醒：

- **【可刷卡】** 账单日次日，当天上午 9 点提醒 —— 新周期开始了
- **【还款】** 还款截止日，在 3 天前、1 天前、当天上午 9 点各提醒一次

---

## 五、数据在哪

存在这台设备的浏览器本地（localStorage），**不上传任何服务器**。

代价是：换设备、换浏览器、清理浏览器缓存，数据就没了。
所以「设置 → 导出备份」会生成一个 JSON 文件，建议：

- 每次录入较多数据后导出一次，扔进 iCloud 云盘或微信收藏
- 换设备时用「导入备份」恢复

---

## 六、以后想升级的话

目前是**集中式单机版**：你一个人管四个人的卡，数据在你的设备上。
之所以这样起步，是因为四台机器是共享资源，必须有一个地方能看到全局的
使用间隔——如果每人各记各的，轮换协调就没法做了。

如果将来需要**朋友各自在自己手机上记录**，需要的改动是：

1. 加一层云端存储（Cloudflare Workers + KV 或 D1 即可，成本几乎为零）
2. 前端把 localStorage 的读写换成 API 调用
3. 加一个简单的口令区分身份

当前的数据结构（people / cards / terminals / txns 四张表）已经是按多人设计的，
搬到云端不需要重构，只是把存储层换掉。在真的遇到「各自记录」的需求之前，
不建议提前做——单文件版可以直接复制给朋友，改起来也快。

---

## 七、两点需要你自己权衡的事

**关于用卡合规**
银行对信用卡资金用途有明确规定，信用卡应当用于真实消费。
非真实交易的资金套取属于违规，一旦被认定，可能面临降额、冻结，
情节严重的还有更进一步的后果。这个工具只负责帮你记账和提醒，
交易本身是否合规需要你自己把握。

**关于零账单的代价**
长期做到零账单，征信报告上确实看不到这些卡的额度占用。
但反过来说，这也意味着这几张卡几乎没有留下用卡记录。
部分银行的提额模型、以及某些贷款审批场景，反而看重稳定的用卡频率和
按时还款历史。完全没有记录，未必总是加分项。
是否长期这么操作，值得你根据自己近期的融资计划来判断。

---

## 附：文件结构

```
信用卡还款/
├── index.html   完整应用，单文件，双击即用
└── 说明.md      本文档
```

`index.html` 内部分区：

| 区段 | 内容 |
|---|---|
| 日期工具 | `nextStmt` / `prevStmt` / `clampDay` —— 账单日在各月的落点计算 |
| 核心计算 | `calc(card)` —— 返回一张卡此刻的完整周期状态，所有界面都基于它 |
| 渲染 | `renderDash` / `renderHist` / `renderPos` / `renderSet` 四个页面 |
| 日历导出 | `exportICS()` —— 生成 12 个月的 .ics |
| 备份 | `exportJSON` / `importJSON` |
