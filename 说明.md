# 卡周期 · 说明

线上地址：**https://qiuyuanqr.github.io/cardcycle/**

管理信用卡账单周期的记录与还款提醒工具。多张卡各有各的账单日，每张卡都要在
账单日之后刷、下个账单日之前还清——这个工具替你记住所有这些错开的循环。

## 数据存在哪（重要）

- 数据只存在**打开网页的那台设备**的浏览器里（localStorage），不上传任何服务器
- GitHub 上的仓库是公开的，但里面**只有程序，没有任何人的数据**
- 每个人打开这个网址，看到的都是自己设备上自己录的数据，互相看不到
- 代价：换手机、清理浏览器数据会丢，所以要定期「设置 → 复制/导出」备份

## iPhone 上怎么装

1. Safari 打开 https://qiuyuanqr.github.io/cardcycle/
2. 点分享按钮（底部中间向上箭头）→「添加到主屏幕」
3. 以后从主屏幕图标进，全屏无地址栏，断网也能打开

**务必从主屏幕图标进，不要每次在 Safari 里现开。**原因：Safari 对普通网站的
本地数据有「7 天不访问就清除」的策略，加到主屏幕后这个限制基本解除，数据更安全。

## 录入自己的数据

首次打开是空的。两种方式：

- **粘贴导入**（快）：设置 → 粘贴/导入 → 把别人发你的备份文字粘进去 → 导入
- **手动添加**：设置 → 添加信用卡，填银行、账单日、提前几天还款

每张卡的「提前几天还款」独立设置，编辑时会实时预览「下次提醒还款是几号」。

## 时间模型

```
     上个账单日          今天             还款截止         下个账单日
 ────────┬───────────────┼────────────────┬────────────────┬─────────→
         │                                │                │
         └─ 次日起：安全刷卡窗口           └── 提前 N 天 ──┘
```

- **安全刷卡窗口**从上个账单日的次日开始。账单日当天刷的钱多数银行计入当期，
  所以账单日当天不要刷（记账时如果临近账单日，弹窗会红字警告）
- **还款截止日** = 下个账单日 − 该卡设置的提前天数。跨行还款可能次日到账，
  账单日当天银行日终结算，卡点还太险
- 每笔刷卡按日期自动归入所属周期；账单日 29/30/31 号在短月自动落到月末

概览页按紧急程度排序：已出账单 / 已超期限（红）→ 今明还清（橙）→
准备还款（黄）→ 周期进行中（绿）→ 本期未刷（灰，收成一行）。

## 四台机器的轮换

机器是共享资源，「机器」页集中显示：

- 按闲置时间排序——最久没用的排最上
- **卡 × 机器矩阵**——每张卡在每台机器上「上次是几天前刷的」，绿色=间隔够了

记一笔时机器已按闲置排序、默认选中最久没用的那台，每台标着「本卡几天前用过」。

## 日历提醒

设置 → 导出日历提醒（.ics）→ iPhone 上选「存储到文件」→ 到文件 App 点开 →
加入日历。每张卡每月两条：

- **【还款】** 各自的提醒日（如广发 = 每月 7 号），3 天前 / 1 天前 / 当天上午 9 点各响一次
- **【可刷卡】** 账单日次日上午 9 点，提示新周期开始

建议新建一个「卡周期」日历分组专门存放，改动账单日后重新导出前先整组删旧的。

## 备份与换机

- 备份：设置 → 复制/导出 → 复制到剪贴板 → 粘贴到备忘录/微信收藏
- 恢复：新设备打开网页 → 设置 → 粘贴/导入
- 概览页会在从未备份或距上次备份超过 30 天时提示

## 单人与多人模式

默认是**单人模式**：界面上完全没有「持卡人」概念，就是你自己的卡，
别人借你手机看到的也只是一份普通的个人记账。

需要替朋友代管卡片时，到「设置 → 参数 → 多人管理」打开开关，
界面才会出现持卡人管理和人名标注。已有多个持卡人的老数据、
或导入多人备份时，会自动进入多人模式。

## 朋友怎么用

把网址发给他们，各自加到主屏幕、各自录自己的卡。数据互不相通。
如果他们的卡也由你统一管理，就都录在你的手机上，开多人模式用「持卡人」区分。

**注意**：四台机器的使用间隔只有在「所有刷卡记录都在同一台设备上」时才算得准。
如果朋友各记各的，每个人只能看到自己那部分的机器使用情况——这种情况下建议
你们约定好各自固定用哪几台，或仍由一个人统一记录。

## 两点提醒

1. 银行对信用卡资金用途有明确规定，信用卡应当用于真实消费。非真实交易的资金
   套取属违规，被认定可能面临降额、冻结乃至更严重后果。本工具只负责记账与提醒。
2. 长期零账单，征信上看不到额度占用，但也意味着这些卡几乎没有用卡记录。部分
   银行的提额模型和某些贷款审批反而看重稳定的用卡与按时还款历史，值得权衡。

## 文件结构

| 文件 | 作用 |
|---|---|
| `index.html` | 完整应用（界面 + 逻辑 + 存储），单文件 |
| `manifest.webmanifest` | PWA 清单：主屏幕图标、全屏模式 |
| `sw.js` | Service Worker：离线缓存（页面网络优先，图标缓存优先） |
| `icon-*.png` | 应用图标 |

改动 `index.html` 后 `git push`，约一分钟后线上生效；已装到主屏幕的用户
下次联网打开时自动拿到新版。
