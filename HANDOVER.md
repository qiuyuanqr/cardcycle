# 卡周期 · 项目交接文档

> **交接对象**：Codex（以**拷贝**方式接管，原仓库与线上服务不变更）
> **交接日期**：2026-08-07
> **一期状态**：**已全部完成**——网页版上线稳定运行；微信小程序 1.0.17 已通过备案与审核、**已发布上线**
> **交接基线**：`main` @ `bb59140`，`npm test` 31 项全过（2026-08-07 复跑确认），工作区干净
> **交接后已修**：Codex 复审报出的 P1（读失败时网页整个起不来，根因 TDZ）已于同日修复上线，
> 复盘见 `docs/AUDIT_PLAN_20260731.md` 的「W-b 复盘」

接手前请把本文读完。第 2 节和第 9 节是**不读会出事**的两节：前者关系到用户的真实卡数据不被带走，
后者是三轮真机排查换来的结论，重踩一遍要花掉好几天。

---

## 0. 项目在哪（一张表说清所有地址）

| 是什么 | 在哪 | 备注 |
|---|---|---|
| **本地项目目录** | `/Users/yangqiuyuan/Coding/信用卡还款/` | 原主的开发目录，macOS。**注意目录名是中文**，命令行里记得加引号 |
| **GitHub 仓库** | https://github.com/qiuyuanqr/cardcycle | **公开**，clone 不需要凭据。SSH：`git@github.com:qiuyuanqr/cardcycle.git` |
| **线上网页版** | https://qiuyuanqr.github.io/cardcycle/ | GitHub Pages，由仓库 `main` 分支自动部署 |
| **微信小程序** | AppID `wx43420b254f2f2bdd` | 1.0.17 **已发布上线**，微信内可搜到；改版需在已登录的微信开发者工具里 `cli upload` |
| **小程序目录** | 仓库内 `miniprogram/` | 用微信开发者工具「导入项目」指向这个子目录，不是仓库根目录 |
| **开发者工具 CLI** | `/Applications/wechatwebdevtools.app/Contents/MacOS/cli` | `preview` / `upload` 都走它 |

> **本地目录和 GitHub 仓库内容一致**（工作区干净，与 `origin/main` 同步），
> 但本地目录里**多出四样不该带走的东西**——见第 2.3 节。
> 所以**优先从 GitHub 克隆**，只有在必须用本地目录时才按 2.2 节的 rsync 排除清单拷。

---

## 1. 这个项目是什么

**卡周期**——信用卡账单周期的记录与还款提醒工具。

典型使用者手上有若干张信用卡，每张卡的账单日各不相同。用卡节奏是：
**账单日之后刷、下个账单日之前还清**。多张卡错开的循环靠脑子记不住，这个工具替他记。

- 记录每笔消费的时间、金额、所用商户
- 按每张卡各自的账单日自动算周期归属、还款截止日、当前状态
- 导出 `.ics` 日历文件，用系统日历做还款提醒（不做应用内推送，原因见第 9 节）
- 商户轮换：显示每张卡在每个商户「上次是几天前刷的」，避免同卡同商户过密

**产品形态上最关键的一条**：数据**只存在用户自己设备的浏览器 / 微信本地存储里**，
不上传任何服务器，仓库里只有程序、没有任何人的数据。备份 = 复制一段 JSON 文本或导出一个文件。
这不是「还没做后端」，是**刻意的设计**——见第 5 节。

面向用户的完整说明在 [`说明.md`](说明.md)，写给使用者看，接手时也值得读一遍来理解产品语义。

---

## 2. 怎么把项目拷过去

### 2.1 推荐做法：从 GitHub 克隆

仓库是**公开**的，不需要任何凭据：

```bash
git clone https://github.com/qiuyuanqr/cardcycle.git cardcycle && cd cardcycle && git remote rename origin upstream
```

这样拿到的是干净的 55 个受版本控制的文件（约 420 KB）+ 38 条提交历史，
`.gitignore` 已经把所有不该带走的东西挡在外面。**把 `origin` 改名成 `upstream`**（或直接删掉），
避免误 push 到原仓库——线上网页版是 push 到该仓库 `main` 触发部署的，误推会直接改动线上。

克隆后先跑一次基线验证：

```bash
npm test
```

期望输出 `pass 31 / fail 0`，随后 `check-sync` 静默通过（`core.js` 与 `miniprogram/utils/core.js` 逐字节一致）。
项目**零运行时依赖**，`npm test` 只用 Node 内置的 `node --test`，不需要 `npm install`。

### 2.2 如果只能整目录拷贝

本地目录是 `/Users/yangqiuyuan/Coding/信用卡还款/`。直接 `cp -r` 会把下面这些一起带走，
必须先排除：

```bash
rsync -a --exclude='.git' --exclude='node_modules' --exclude='.claude' \
      --exclude='.DS_Store' --exclude='我的数据-粘贴用.txt' \
      --exclude='miniprogram/project.private.config.json' \
      '/Users/yangqiuyuan/Coding/信用卡还款/' ./cardcycle/
```

### 2.3 ⛔ 绝对不能带走的东西

| 文件 | 为什么 |
|---|---|
| **`我的数据-粘贴用.txt`** | **用户 7 张卡的真实数据**（银行、账单日、卡号后四位、消费流水）。已在 `.gitignore` 里，所以 git 克隆不会带；**但整目录拷贝会带**。接管方不需要也不应该持有这份数据 |
| `node_modules/` | 54 个包的残留（当年生成图标时装的 `jimp`），`package.json` 里**没有任何 dependencies**，纯垃圾，删掉即可 |
| `.claude/` | 原主的本地开发配置（一个 `python3 -m http.server 8791` 的启动项），与项目无关 |
| `.DS_Store` | macOS 垃圾文件 |
| `miniprogram/project.private.config.json` | 微信开发者工具的本地私有配置（当前不存在，`.gitignore` 已预留） |

**这条规矩要延续下去**：仓库是公开的（免费账户开不了私有仓库的 Pages），
**任何个人数据都绝不能 commit**。每次提交前 `git status` 确认一眼。

### 2.4 交接不过去的东西（留在原主手上）

拷贝能带走代码，带不走这些。接手后遇到相关需求，必须回头找项目所有者操作：

| 资产 | 说明 |
|---|---|
| GitHub 仓库 `qiuyuanqr/cardcycle` 的写权限 | 线上网页版 https://qiuyuanqr.github.io/cardcycle/ 由该仓库 `main` 分支的 GitHub Pages 提供。**副本 push 不会影响线上**；要发布必须由所有者推，或另建自己的 Pages 站点 |
| 微信小程序账号（AppID `wx43420b254f2f2bdd`） | 个人主体，已完成微信认证、类目已定、《用户隐私保护指引》已配置。`cli upload` 需要在**已登录该账号的微信开发者工具**里执行 |
| **AppSecret** | 不随仓库交接，仓库里也没有。项目所有者已决定**不做轮换**；接手方若接管账号请自行决定 |
| ICP 备案 | ✅ 已通过，主体是项目所有者。小程序已提审通过并发布上线 |
| 已上线用户的本地数据 | 在各自的手机里，谁也拷不走。**这也意味着一次数据事故就是用户全部账单记录**——见第 5 节的底线 |

---

## 3. 架构：一核三端

这个项目最值钱的资产不是界面，是**「周期归属和还款日怎么算」这套规则**。
所以架构的第一原则：**核心规则只写一份，谁都来复用它**。

```
                    ┌─────────────────────────┐
                    │        core.js          │
                    │  纯函数 · 零依赖 · 无界面  │
                    │ ─────────────────────── │
                    │ 日期数学  nextStmt/prevStmt│
                    │ 周期计算  calc            │
                    │ 日历生成  buildICS        │
                    └───────────┬─────────────┘
              ┌─────────────────┼─────────────────┐
      ┌───────┴──────┐  ┌───────┴──────┐  ┌───────┴──────┐
      │  网页版 PWA   │  │  微信小程序    │  │  测试套件     │
      │  index.html  │  │ miniprogram/ │  │  test/       │
      │  localStorage │  │  wx.storage  │  │  node --test │
      └──────────────┘  └──────────────┘  └──────────────┘
```

`core.js` 是 UMD 包装：浏览器里挂全局 `CardCycleCore`，Node/小程序里走 `module.exports`。
380 行，导出 25 个纯函数 + 1 个常量，**没有任何 I/O、没有任何界面代码**。

### 铁律（改代码前必读，四条都不是建议）

1. **任何涉及日期、周期、金额归属的逻辑，只允许写在 `core.js`。**
   界面层（`index.html`、小程序页面）只做两件事：把状态喂给 core、把 core 的结果画出来。
   在界面层重算一遍周期，是这个项目最严重的架构违规。
2. **改 `core.js` 必须先跑 `npm test`。** CI（`.github/workflows/test.yml`）在 push 到 `main`
   和所有 PR 上自动跑，红了不发布。
3. **`core.js` 有两份拷贝，改完必须同步。**
   `miniprogram/utils/core.js` 是 `core.js` 的副本（小程序不支持跨目录 require 出包）。
   改完执行 `npm run sync-core`（就是一条 `cp`），`npm test` 末尾的 `check-sync` 会用
   `diff -q` 拦住不一致。**永远只改根目录那份，然后 sync 过去，不要反过来。**
4. **存储层永远隔离在薄适配层里。** 网页是 `index.html` 内的 `load/save`，
   小程序是 `miniprogram/utils/store.js`。换存储不动逻辑。

---

## 4. 目录地图

```
cardcycle/
├── core.js                      # ★ 核心领域逻辑（380 行，UMD，零依赖）
├── index.html                   # 网页版完整应用（927 行单文件：样式 + UI + localStorage 适配）
├── sw.js                        # Service Worker：离线缓存
├── manifest.webmanifest         # PWA 清单：主屏幕图标、全屏模式
├── icon-{180,192,512}.png       # 应用图标
├── package.json                 # 只有 scripts，无依赖
├── .github/workflows/test.yml   # CI：push→main + PR 时跑 npm test
├── test/
│   ├── core.test.js             # 21 项：月份天数/闰年/月末/跨年/周期/冲抵/ICS
│   └── store.test.js            # 10 项：存储层（手写 wx mock，测「数据能不能活下来」）
├── docs/
│   ├── 架构与小程序路线.md        # ★ 架构原则、里程碑表、开发环境备忘、踩坑记录
│   └── AUDIT_PLAN_20260731.md   # ★ 全库代码审查的结论与修复记录（4 批次）
├── README.md                    # 仓库门面（简短）
├── 说明.md                       # ★ 面向使用者的完整说明（产品语义的权威来源）
├── HANDOVER.md                  # 本文
└── miniprogram/                 # 微信小程序本地版
    ├── app.json / app.js / app.wxss / sitemap.json
    ├── project.config.json      # AppID、libVersion（必须是有效版本或 latest）
    ├── utils/
    │   ├── core.js              # ← core.js 的副本，勿单独编辑
    │   └── store.js             # 存储适配 + 把 core 结果转成 WXML 能直接绑的纯数据
    ├── components/repay/        # 自绘还款弹窗（不能用原生 showModal，见第 9 节）
    └── pages/
        ├── dash/                # 概览（tabBar）
        ├── hist/                # 流水（tabBar）
        ├── settings/            # 设置（tabBar）
        ├── swipe/               # 记消费
        ├── card/                # 卡片编辑
        ├── cardtx/              # 单卡明细
        └── pos/                 # 商户管理（从设置页进入，不在 tabBar）
```

**读代码的建议顺序**：`说明.md`（懂产品）→ `core.js` 的 `calc`（懂核心）→
`test/core.test.js`（懂边界）→ `miniprogram/utils/store.js`（懂适配层长什么样）→
`index.html`（网页版全貌）。

---

## 5. 数据模型与存储契约

### 数据结构（两端完全一致）

```
people    [{id, name}]                                  # 持卡人（单人模式下只有一个隐形的「我」）
cards     [{id, personId, bank, last4,
            statementDay 1-31, buffer 提前天数, limit}]
terminals [{id, name, note}]                            # 商户
txns      [{id, cardId, terminalId, date, amount, note, ts}]   # 消费流水（ts = 记账时刻毫秒）
payments  [{id, cardId, date, amount, ts}]              # 还款流水
settings  {buffer, minGap, multiUser, lastBackup, cardSort, swipeStyle}
```

存储 key 两端都是 **`cardcycle.v1`**。备份就是这份结构的 JSON 文本——
**所以网页版的备份可以直接粘进小程序，反之亦然**，这是两端互导数据的唯一通道，别弄坏。

### 三条业务契约

1. **消费与还款是两本独立的账。** 没有「这笔还的是哪一笔消费」的对应关系。
   **待还 = 消费总额 − 还款总额**，随时现算。`calc` 内部让还款先冲抵最早的消费，
   那只是为了判断周期归属，不是账目关系。
   （2.2 以前是逐笔标 `repaid`，旧数据由 `core.migrateRepaid` 自动迁移，金额守恒。）
2. **还款不封顶**，超出待还的部分记为**存款**（`calc` 返回 `deposit`），下次消费自动抵扣。
3. **单人 / 多人是同一份代码的显示开关**（`settings.multiUser`），不是两个版本。
   默认单人，界面完全不出现「持卡人」；导入多人备份或老数据有多个持卡人时自动切多人。

### 存储层的底线（这一条最重要）

> **任何「读不到数据」的分支都绝不允许顺手写盘。**

历史上出过一次真事故：`store.load()` 把 `getStorageSync` 的异常吞掉后 `d` 为 null，
于是**当成首次启动、立刻写入空种子，把盘上的真数据覆盖掉**，全程无提示且不可逆。

现在的防线是三层，改存储层时不许拆：

1. `readRaw()` 区分「读到空」与「读失败」，失败重试一次；
2. 读失败返回带 `readFailed`（网页版叫 `loadFailed`）标记的空状态，**不写盘、不种种子**；
3. `save()` 拒绝写回任何带该标记的状态，返回 `false`；上层（记消费 / 登记还款 / 保存卡片）
   拿到 `false` 就**回滚内存并如实报错**，不再出现「提示已保存、其实没写进去」；
   导出备份在读失败时也拒绝导出（否则用户会拿一个空壳覆盖掉好备份）。

**唯一的例外**：导入和清空是用户明确的覆盖动作，会**解除**拦截——那是读失败后的恢复路径，别堵死。

第四层是**可达性**：读失败时不但不能写盘，**还必须保证用户走得到恢复路径**。
网页版曾经在这里栽过——`loadFailed` 声明在 `let S=load()` 之后，读失败时 `catch` 给它赋值
撞上 TDZ 抛 ReferenceError，整个脚本从第一行就中断，页面成了点不动的空壳，
数据没丢却拿不回来（2026-08-07 修复，复盘见 `docs/AUDIT_PLAN_20260731.md`）。
**改这一层时，请对着「存储不可读」「数据损坏」「真·首次打开」「正常有数据」四个场景各跑一遍**，
不要因为「已经包了 try」就认定兜住了。

另有一个安全阀：`core.pruneOrphans`（清理孤儿还款）在 **cards 为空时原样返回**，
因为「没有卡」既可能是真没有，也可能是读取异常拿到空壳，后者若按孤儿清理会把全部流水删光。

**这个项目丢一次数据 = 用户全部账单记录，宁可报错也不要静默恢复默认值。**

---

## 6. 开发与验证

### 跑测试

```bash
npm test
```

= `node --test test/*.test.js` + `npm run check-sync`。31 项，跑完约 150ms。
**这是唯一的自动化验证手段**，改任何核心逻辑前后都要跑。

### 本地预览网页版

`index.html` 里用了 `<script src="core.js">`，`file://` 直接打开会被 CORS 挡，必须起个静态服务：

```bash
python3 -m http.server 8791 --bind 127.0.0.1
```

然后访问 http://127.0.0.1:8791/ 。

### 小程序调试

需要微信开发者工具（macOS），导入 `miniprogram/` 目录。CLI 在
`/Applications/wechatwebdevtools.app/Contents/MacOS/cli`。

生成真机预览码：

```bash
/Applications/wechatwebdevtools.app/Contents/MacOS/cli preview --project <仓库绝对路径>/miniprogram --qr-format image --qr-output /tmp/qr.png
```

**预览码 25 分钟过期**（微信的固定行为，改不了）。长期真机测试要用体验版，见第 7.2 节。

### 测试策略

`test/core.test.js` 测的是**算得对不对**：31 账单日 × 800 天全扫描、闰年、月末落点、跨年、
还款冲抵的分单位整数运算、ICS 折行的 73 字节边界、VALARM 触发换算。

`test/store.test.js` 测的是**数据能不能活下来**：读失败不覆盖、备份往返无损、截断的备份必须报错、
删卡级联、孤儿自愈、字符串被当对象展开的防护。用手写的 `wx` mock。

> **教训（写在这里免得重犯）**：验备份别拿空数据验。
> 第一份真机备份验证 18 项全过、但流水是 0 笔——看着漂亮，其实什么都没证明。
> 后来用 2 笔消费 + 2 笔还款重验才真正验到关键项。**校验必须打出样本规模。**

---

## 7. 部署

### 7.1 网页版（GitHub Pages）

**push 到 `main` 即上线，约 1 分钟生效。** 已装到主屏幕的用户下次联网打开时自动拿到新版。

没有构建步骤——`index.html` 是纯静态单文件。`sw.js` 的缓存策略是
**页面与 core.js 网络优先、图标缓存优先**，且只缓存 `res.ok` 的响应
（否则部署间隙的错误页会顶掉离线缓存里的好版本）。改动这两者时注意 `CACHE` 版本号要动。

⚠️ **接管方的副本 push 不会影响线上**——线上由原仓库提供服务。

### 7.2 小程序

```bash
/Applications/wechatwebdevtools.app/Contents/MacOS/cli upload --project <仓库绝对路径>/miniprogram -v 1.0.18 -d "改动说明"
```

**每次改 `-v` 时，必须同步改 `miniprogram/utils/store.js` 里的 `APP_VERSION` 常量**（当前 `1.0.17`）。
原因：`getAccountInfoSync().miniProgram.version` 在开发版/体验版返回**空字符串**，靠它判断不了
手上跑的是哪一版，所以硬编码一份显示在设置页的「本机存储」板块里。

体验版机制：首次要去 mp.weixin.qq.com → 管理 → 版本管理 → 开发版本 →「选为体验版」点一次；
**之后每次 `cli upload` 体验版会自动跟到最新，进小程序自动刷新，不需要再去后台重选**
（这一条是真机实测更正过的，早期文档写错过）。体验成员在 成员管理 → 体验成员 里加，上限 90 人。

---

## 8. 小程序上架现状与卡点

| 项 | 状态 |
|---|---|
| 微信认证 | ✅ 已完成 |
| 类目 | ✅ 已定 |
| 《用户隐私保护指引》 | ✅ 已配置（2026-08-03） |
| ICP 备案 | ⏳ 进行中，**提审的前置条件** |
| 真机验证 | ✅ 完成（详见第 10 节） |
| 提审 | ✅ 已通过 |
| **发布** | ✅ **已发布上线，用户可搜索到**（2026-08-07 确认） |

> **一期到此全部完成**：网页版与小程序都已上线。下面关于提审的内容转为**历史记录**保留——
> 后续版本迭代重新提审时仍然适用（隐私指引、类目风险、首审面积）。

**隐私保护指引命中两类**，配错会被拦在提审门口：

- **「读取你的剪切板」**——`setClipboardData` / `getClipboardData`。
  注意：**复制出去也算这一类**，别为了免声明而砍掉剪贴板备份，那是网页 ⇄ 小程序互导数据的通道。
- **「收集你选中的文件」**——`chooseMessageFile`（从聊天文件恢复备份）。

`shareFileMessage`（转发文件）与 `.ics` 导出**不在**隐私清单里。
指引本身有独立的快速审核，生效后首次调用接口时微信自动弹官方授权半屏弹窗，**代码无需改动**。

**过审的现实风险（提前知道）**：「信用卡管理」在微信的语境里容易被判定为**金融信息服务**，
那需要企业主体 + 金融资质，个人主体给不出。现实做法是以「效率-记账/提醒」类工具提审，
功能如实描述为「记录消费、提醒还款日」。**个人主体做卡片管理类工具被驳回是常见情况**，
要有心理预期。不建议为了过审在名称和描述里伪装功能，违规被发现会封号。

首审建议只带核心功能（卡片周期 + 记账 + 提醒），商户轮换管理后续版本再说——
不是隐瞒，是最小化首审面积。

---

## 9. 踩过的坑（别再踩一遍）

### 9.1 安卓批量写手机日历：机制上不可行，已放弃 ⛔

三轮真机排查的结论。`wx.addPhoneCalendar`：

- 串行回调链 / `setTimeout` 从第二条起报 `can only be invoked by user TAP gesture`；
- 点击链里同步并发 84 条：**每条都要用户手动点一次「创建日历」系统确认框**，
  并发时只弹前 1–2 个框，**其余 82 条没写进去也照样回调 `success`**（回调计数完全不可信）。

即批量写 = 让用户点 84 次确认，且点了也未必真写。
**1.0.11 起「写入手机日历」功能整个下线**（用户拍板：别搞来搞去），提醒只保留「导出 .ics」——
一次导入全部、还款自带 3 天前/1 天前/当天三响、全平台一致。

> **将来别再试图恢复批量写日历。** 这是产品决策，不是技术债。

同一个坑：`shareFileMessage` 也必须留在点击调用链里，放进异步回调会静默失败。

### 9.2 「扫码后数据有时有有时没有」——绝大多数不是 bug

**开发版（预览码）/ 体验版（体验码）/ 正式版是三个互不相通的本地存储空间**，
扫不同来源的码进去看到的就是不同的数据，且换版本不清空。

2.3 起设置页有「本机存储」板块，直接显示当前 `envVersion`、本环境的卡与流水条数、占用大小。
**下次再有人喊「数据没了」，先看这里是哪个环境**，一眼分清是走错空间还是真丢了。
跨空间搬数据用「复制备份 / 从剪贴板导入」。

推论：**首启种子数据只在存储为空时写入，改种子内容对老设备无效**——
要改必须在 `normalize` 里加迁移。先例：`core.pruneSeedTerminals` 清理 2.1 老设备种下的
A–D 占位商户（只动「旧占位名 + 无备注 + 从未记账」的）。

### 9.3 微信开发者工具的环境坑

- 工具是 **Electron 新版（2.02+）**，配置目录 `~/Library/Application Support/微信开发者工具/<hash>/`。
- **CLI 报「服务端口已关闭」**：端口设置存在 `<hash>/WeappLocalData/localstorage_b72da75d….json`
  （`reduxPersist:settings` → `security.enableServicePort`），可直接改此文件后重启工具。
- **`project.config.json` 的 `libVersion` 必须是有效版本或 `latest`**——无效版本会导致模拟器
  基础库加载失败，以及 preview/upload 报 `41002 appid missing`（**报错信息完全是误导**，
  跟 appid 没关系，别顺着它查）。
- 新版工具对 `miniprogram-automator` 自动化桥**不响应**（命令超时），
  运行时验证以 `cli preview`（编译+上传链路）+ 真机为准，别指望自动化 UI 测试。

### 9.4 其它零散但会浪费时间的

- **原生 `wx.showModal` 的 `editable` 会把 `content` 预填进输入框**，所以还款弹窗是自绘组件
  （`components/repay`）。别改回原生。
- **`||` 判空会吞掉 0**：提前天数 = 0、商户最小间隔 = 0 都是合法值。
  全项目统一用 `== null` 判空，这个坑修过两轮（网页版先修，小程序端 1.0.15 才补齐）。
- **Safari 对普通网站的本地数据有「7 天不访问就清除」策略**，所以必须引导用户
  **加到主屏幕后从图标进**，加了之后这个限制基本解除。这是产品说明里的硬要求，不是可选建议。

---

## 10. 当前进度与待办

### 里程碑

| 阶段 | 内容 | 状态 |
|---|---|---|
| 1.0 | 网页 PWA + 本地存储 + ICS 提醒 | ✅ 已上线 |
| 2.0 | `core.js` 抽离 + 测试/CI + 单人模式 | ✅ 完成 |
| 2.1 | 小程序本地版（复用 core.js） | ✅ 完成 |
| 2.2 | 体验打磨：单卡明细页、消费/还款两本账、存款抵扣、概览排序 | ✅ 完成 |
| 2.3 | 数据安全专项：读失败不覆盖、删卡级联、孤儿自愈、`store.test.js` | ✅ 完成，备份链路真机验证通过 |
| 2.4 | 全库代码审查（4 批次）：P0/P1 零发现，4 项 P2 已修 | ✅ 完成 |
| 2.5 | 外部复审修复：读失败时网页 TDZ 崩溃（P1） | ✅ 完成 |
| — | **小程序备案 → 提审 → 发布** | ✅ **已上线，用户可搜索到** |
| 3.0 | 云开发同步版 | 📋 规划中，未动工 |

详细的每阶段改动清单在 [`docs/架构与小程序路线.md`](docs/架构与小程序路线.md) 的里程碑表。
审查结论与修复记录在 [`docs/AUDIT_PLAN_20260731.md`](docs/AUDIT_PLAN_20260731.md)。

### 一期收尾（已全部结清，2026-08-07）

1. ~~真机验证 3 项~~ → 已结清：
   - 设置页「本机存储」面板显示 ✅
   - 删卡级联删还款的提示文案 ✅
   - **同日多笔流水的排序** ✅ —— 这一项本来就不是「规则待定」，规则早已定死在
     `core.txnCmp` 并有测试固化，见下方「同日多笔排序的既定规则」。
2. ~~提审前重置 AppSecret~~ → **项目所有者决定不做**（评估后认为成本高于收益）。
   接手方如果要接管小程序账号，请自行决定是否轮换。
3. ~~等 ICP 备案~~ → ✅ 已通过，且**已提审、已发布上线**。

### 同日多笔排序的既定规则（`core.txnCmp`）

流水列表（消费与还款混排）三级排序，两端共用同一个纯函数：

| 级别 | 规则 | 为什么 |
|---|---|---|
| 1 | **日期倒序** | 用户找账先看哪天 |
| 2 | 同日内**按记账时刻 `ts` 倒序** | 刚记的排最上；口径与概览「最近变动」一致 |
| 3 | 全打平时**按 `id` 倒序兜底** | 保证同一份数据每次渲染顺序都一样，不随机跳动 |

这里要分清**两个时间**：`date` 是「这笔消费发生在哪天」（只精确到天），
`ts` 是「用户按下保存的那一刻」（毫秒）。老记录**只有 `date` 没有 `ts`**，
退回「当天 0 点」参与比较，因此沉在同日新记录之后。

**唯一定不下来、也永远定不下来的**：同一天里的多条**老记录**之间，真实先后
**无法恢复**——当年的数据结构里就没有记账时刻这个字段，信息不是丢了，是从来没存过。
现在按 `id` 兜底，保证同一份数据每次渲染顺序都一样（不跳动），
但那个顺序**不代表真实先后**。这是数据缺失，不是规则未定，别再花时间"定"它。

**影响范围**：`ts` 是 `9a6f361`（2026-07-27 16:13）引入的，之后四条写入路径
（网页/小程序 × 消费/还款）全部写 `ts: Date.now()`。**新使用者自己录的数据一律带 `ts`，
不受此限制。** 缺 `ts` 的记录只有两个来源：

1. 该提交之前录入的记录（App 上线首日约 15 小时的窗口）；
2. **导入 2.2 之前格式的备份**——`migrateRepaid` 把逐笔 `repaid` 标记折算成还款流水时，
   只拿得到 `repaidDate`（哪一天），拿不到当时的记账时刻，生成的还款记录同样没有 `ts`。

**不要在 `normalize` 里给缺 `ts` 的记录回填「当天 0 点」**：那正是 `txnCmp` 排序时
已经在算的 fallback，回填进数据没有任何信息增益，反而会抹掉「这条记录本来就没有
真实记账时刻」这个事实，以后再想分辨就分辨不出来了。**兜底只在排序那一刻算，不写进数据。**

**而且它只影响显示顺序，不碰任何计算**：待还 = 消费总额 − 还款总额，与顺序无关；
周期归属和还款日看的是 `date` 不是 `ts`。老记录排反了也不会算错一分钱、错一天。

### 已知但决定暂不处理的（备注级，非缺陷）

来自 2.4 审查，编号与 `docs/AUDIT_PLAN_20260731.md` 一致：

- **W-a** 备份导入未做字段级校验——`normalize` 只保证五个字段是数组，不校验元素内部；
  构造过的备份文字可造成自 XSS（需用户自己粘贴恶意备份，风险有限）。
  可在 `normalize` 里做字段类型清洗一并解决。
- ~~**W-b** 浏览器完全禁用存储时启动中断~~ → **已于 2026-08-07 修复**，
  且真正的根因比这条描述深一层（TDZ，断点在 `let S=load()` 而非启动尾部）。
  复盘见 `docs/AUDIT_PLAN_20260731.md` 的「W-b 复盘」——那一节也是**这个项目的存储层
  为什么必须逐个失败场景实测**的最好例子。
- **W-c** 清空数据的确认文案列了卡/商户/消费数，未提还款笔数（两端同）。
- **M-a** 次要写路径（加/删商户、删单条流水、删卡等）不检查 `save()` 返回值；
  写失败时界面短暂显示成功，但各页 `onShow` 都会重新 `load()`，下次进入即自我纠正。
  2.3 有意只给三大主路径做回滚。
- **E-a** CI 仅在 push→`main` 与 PR 上触发，架构文档写的是「每次 push 自动跑」，
  文档与配置至少对齐一个。
- **E-b** `project.config.json` 里的 appid 不是机密（appsecret 才是），公开仓库可接受。

### 3.0 云开发（若要继续做，先知道这些）

要实现「**共享商户状态**」（所有人的记录一起算使用间隔——这是「一起用」的核心价值，
本地版做不到）、多设备同步、订阅消息定时提醒，必须上后端。

选**微信云开发**而非自建：小程序的网络请求域名必须是**已 ICP 备案的 https 域名**，
自建路线要买国内服务器 + 域名备案。云开发不受此限制（走微信自己的通道），代价是绑定微信生态。

**费用如实**：2024 年起微信云开发取消免费额度，基础套餐约 **19.9 元/月**，这是固定成本。

隐私设计要延续：云端按 `openid` 隔离，每人只能读写自己的数据；
商户使用记录只共享「哪台机器、哪天、用没用」，**不共享金额和卡片明细**。
应用里永远只存银行名、卡号后四位、账单日、金额——**不存完整卡号、不存 CVV、
不存任何能操作卡片的信息**。

---

## 11. 接手第一天的验收清单

按顺序做完，就算完成交接：

- [ ] 克隆仓库，确认 `git remote` 里**没有**指向原仓库的 `origin`
- [ ] 确认 `我的数据-粘贴用.txt` **不在**你的副本里
- [ ] `npm test` → 期望 `pass 31 / fail 0`，且 `check-sync` 无输出
- [ ] 起本地静态服务打开 `index.html`，手动加一张卡 + 记一笔消费 + 登记一次还款，
      确认概览状态和待还金额符合预期
- [ ] 读完 `说明.md`（产品语义）与 `core.js` 的 `calc`（核心算法）
- [ ] 做一次「改 `core.js` → `npm run sync-core` → `npm test`」的空转，
      确认你掌握了第 3 节的铁律 3
- [ ] 确认你**没有**、也不需要小程序账号和线上仓库的写权限；
      涉及发布的动作一律回头找项目所有者

---

## 附录 A：交给 Codex 的开场白（直接复制这段）

下面这段是**项目所有者对接手方说的第一句话**，整段复制粘贴即可，不用改。
方括号里的两处按实际情况二选一。

```text
接手一个已经开发完一期的项目，叫「卡周期」——信用卡账单周期记录与还款提醒工具。
以拷贝的方式接管：原仓库和线上服务都不动，你在自己的副本上工作。

项目位置：
- 本地目录：/Users/yangqiuyuan/Coding/信用卡还款/   （目录名是中文，命令行记得加引号）
- GitHub 仓库（公开，无需凭据）：https://github.com/qiuyuanqr/cardcycle
- 线上网页版：https://qiuyuanqr.github.io/cardcycle/
- 微信小程序 AppID：wx43420b254f2f2bdd（开发版 1.0.17，未发布）

第一件事：读仓库根目录的 HANDOVER.md，那是完整的交接文档。
读完再动手，不要先看代码。文档里第 2 节和第 9 节必须读——
第 2 节讲哪些文件绝对不能带走（有用户真实的信用卡数据），
第 9 节是三轮真机排查换来的踩坑结论，重踩一遍要花好几天。

怎么拷：
[ 优先 ] git clone https://github.com/qiuyuanqr/cardcycle.git 到你自己的工作目录，
        然后把 origin 改名成 upstream 或直接删掉，避免误 push 到原仓库。
[ 或者 ] 如果必须从本地目录拷，按 HANDOVER.md 第 2.2 节的 rsync 命令来，
        那条命令里的排除项一个都不能少。

拷完先跑 npm test 验基线，期望 pass 31 / fail 0，check-sync 无输出。
项目零运行时依赖，不需要 npm install。

三条硬规矩，请你也遵守：
1. 涉及日期、周期、金额归属的逻辑只准写在 core.js，界面层只做适配和渲染；
   改完 core.js 必须跑 npm run sync-core 同步到 miniprogram/utils/core.js，再跑 npm test。
2. 任何「读不到数据」的分支都绝不允许顺手写盘。这个项目丢一次数据
   就是用户全部的账单记录，宁可报错也不要静默恢复默认值。
3. 仓库是公开的，个人数据一个字节都不许 commit。提交前 git status 看一眼。

你没有、也不需要这些权限，涉及发布的动作一律回来找我：
线上仓库的写权限、微信小程序账号、AppSecret、ICP 备案。

读完 HANDOVER.md 后先回我三件事：
① 你打算怎么拷（clone 还是本地目录）；
② npm test 的实际输出；
③ 文档里有没有讲不清楚、你需要我补充的地方。
先别改任何代码。
```

### 为什么这么说

- **先给位置再给文档**——不给绝对路径，接手方会去猜或去搜，中文目录名尤其容易出岔子。
- **明确「读完再动手，不要先看代码」**——这个项目的坑（安卓写日历、三环境存储隔离、
  `41002` 误导性报错）光看代码看不出来，都写在文档里。
- **点名第 2 节和第 9 节**——一份十几节的文档，不点名就等于没重点。
- **要求先回三件事再动手**——`npm test` 的实际输出是可验证的交接凭据，
  「文档哪里不清楚」是让接手方证明自己真读了。
- **明写「先别改任何代码」**——避免热心的接手方一上来就"顺手优化"，
  而第 10 节列的那些备注级问题（W/M/E 系列）是**有意暂不处理**的，不是漏掉的。

---

## 附：一句话版本

> 核心规则只写在 `core.js`，改了必跑 `npm test` 并 `sync-core`；
> 读不到数据时宁可报错也不写盘；仓库公开，个人数据一个字节都不许 commit。
