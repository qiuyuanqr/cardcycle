# 卡周期 · 架构设计与小程序路线

更新：2026-07-27

## 一、架构原则

这个项目最值钱的资产不是界面，是**"周期归属和还款日怎么算"这套规则**。
所以架构的第一原则：**核心规则只写一份，谁都来复用它**。

```
                    ┌─────────────────────────┐
                    │        core.js          │
                    │  纯函数 · 零依赖 · 无界面  │
                    │ ─────────────────────── │
                    │ 日期数学  nextStmt/prevStmt│
                    │ 周期计算  calc            │
                    │ 日历生成  buildICS        │
                    └───────────┬─────────────┘
              ┌─────────────────┼─────────────────┐
              │                 │                 │
      ┌───────┴──────┐  ┌───────┴──────┐  ┌───────┴──────┐
      │  网页版 PWA   │  │  微信小程序    │  │  测试套件     │
      │  index.html  │  │ （二阶段）     │  │  test/       │
      │  localStorage │  │  wx.storage → │  │  node --test │
      │              │  │  云开发(三阶段) │  │  CI 自动跑    │
      └──────────────┘  └──────────────┘  └──────────────┘
```

**规矩（今后开发都遵守）：**

1. 任何涉及日期、周期、金额归属的逻辑，只允许写在 `core.js`。
   界面层（index.html、将来的小程序页面）只做两件事：把状态喂给 core、把 core 的结果画出来。
2. 改 `core.js` 必须先过 `npm test`（GitHub Actions 在每次 push 时自动跑，红了不发布）。
3. 存储层永远隔离在一个薄薄的适配层里（网页是 localStorage，小程序是 wx.storage，
   将来云端是 API）——换存储不动逻辑。

## 二、当前仓库结构

```
cardcycle/
├── core.js                  # ★ 核心领域逻辑（UMD：浏览器全局 / require 双兼容）
├── index.html               # 网页版：UI + localStorage 适配
├── sw.js                    # 离线缓存（页面与 core.js 网络优先，图标缓存优先）
├── manifest.webmanifest     # PWA 清单
├── icon-*.png               # 图标
├── test/core.test.js        # 测试套件（月份天数/闰年/月末/跨年/周期/ICS 全覆盖）
├── package.json             # npm test
├── .github/workflows/test.yml   # CI：push 即测
├── docs/                    # 文档
└── miniprogram/             # （将来）小程序目录，复制或软链 core.js
```

## 三、数据模型（两端一致）

```
people    [{id, name}]                          # 持卡人（单人模式下只有隐形的一个）
cards     [{id, personId, bank, last4,
            statementDay 1-31, buffer 提前天数, limit}]
terminals [{id, name, note}]                    # 刷卡机
txns      [{id, cardId, terminalId, date,
            amount, note, repaid, repaidDate}]  # 交易
settings  {buffer 默认提前天数, minGap 同机同卡间隔, multiUser, lastBackup}
```

单人/多人是**同一份代码的显示开关**（settings.multiUser），不是两个版本：
- 默认单人：界面完全不出现"持卡人"，卡自动归属一个隐形的"我"
- 打开多人：出现持卡人管理，卡片、流水、日历里标注人名
- 老数据（已有多个持卡人）自动进多人模式，导入备份同理

## 四、小程序路线：分三个阶段

### 阶段一（现在）：网页版就是产品
朋友用网址 + 加主屏幕。零成本、零审核、随时更新。
**这个阶段的作用是把功能打磨到位**——小程序审核一次 1-7 天，
不适合频繁试错；网页版改完 push 一分钟上线。

### 阶段二：小程序「本地版」
把网页版原样搬进小程序：core.js 直接 require，存储从 localStorage
换成 wx.setStorageSync，界面用小程序原生组件重写（WXML/WXSS）。

- **零后端、零服务器费用**——数据和网页版一样存在各自手机里
- 不需要备案域名（没有网络请求）
- 提醒改用小程序的**订阅消息**？不行——订阅消息需要后端定时下发，
  本地版做不到。替代：保留 .ics 日历导出（小程序可以用
  wx.shareFileMessage 把文件发出去），或引导用户用手机日历
- 功能上和网页版等价，收益是：微信里点开即用、朋友之间转发方便

### 阶段三：云开发「同步版」（真正的"一起用"）
接入微信云开发（云函数 + 云数据库），才能实现：

- **共享机器状态**：四台 POS 机的使用间隔基于所有人的记录计算——
  这是"一起用"的核心价值，本地版做不到
- 多设备同步、换手机不丢数据
- **订阅消息定时提醒**：云函数每天早上跑一遍所有人的周期，
  该还款的下发微信服务通知——比日历提醒更顺手
- 登录直接用微信身份（wx.login → openid），不用注册账号

**为什么选云开发而不是自建后端**：小程序的网络请求域名必须是
**已 ICP 备案的 https 域名**。你现有的 Cloudflare 域名没有备案，
走自建路线需要买国内服务器 + 域名备案（个人备案通常 2-4 周）。
云开发不受此限制（微信自己的通道），代价是绑定微信生态。

**云开发费用（如实）**：2024 年起微信云开发取消了免费额度，
基础套餐约 19.9 元/月。这是阶段三的固定成本，几个人分摊问题不大，
但值得在动手前知道。

### 数据隐私设计（三阶段都适用）

- 应用里**永远只存**：银行名、卡号后四位、账单日、金额。
  不存完整卡号、不存 CVV、不存任何能操作卡片的信息
- 云端版按 openid 隔离：每人只能读写自己的数据；
  机器使用记录只共享"哪台机器、哪天、用没用"，不共享金额和卡片明细

## 五、小程序上架流程（如实版）

### 流程本身

1. **注册**：mp.weixin.qq.com → 注册小程序 → 主体选"个人"（免费，
   需要身份证、手机号、一个没注册过公众号/小程序的邮箱）
2. **拿 AppID**：注册完成后在"设置-账号信息"里
3. **装开发工具**：微信开发者工具（macOS 版），导入项目填 AppID
4. **开发调试**：工具里有模拟器，真机用"预览"扫码
5. **提审**：工具里"上传"→ 后台"版本管理"→ 提交审核，
   填类目、功能描述、测试账号说明。
   **《用户隐私保护指引》要先配好**（设置→服务内容声明），否则提审被拦：
   本项目命中两类——「读取你的剪切板」（`setClipboardData`/`getClipboardData`，
   **复制出去也算这类**，对应复制备份/剪贴板恢复）与「收集你选中的文件」
   （`chooseMessageFile`，对应从聊天文件恢复备份）；`shareFileMessage` 转发
   文件与 .ics 导出**不在**隐私清单里。指引本身有独立快速审核，生效后
   首次调用接口时微信自动弹官方授权半屏弹窗，代码无需改动。
   已于 2026-08-03 配置完成（1.0.17 时）
6. **审核**：首次一般 1-7 天。驳回会给理由，改完可再提
7. **发布**：审核通过后手动发布，用户即可搜到

### 三个现实门槛（重要，先想清楚再投入）

**门槛一：类目。** 个人主体可选的类目有限（工具、生活服务的部分子类等）。
"信用卡管理"在微信的语境里容易被判定为**金融信息服务**，
那需要企业主体 + 金融相关资质，个人主体给不出。
现实做法是以"效率-记账/提醒"类工具提审，功能如实描述为
"记录消费、提醒还款日"。**能否通过取决于审核员的判定，
个人主体做卡片管理类工具被驳回是常见情况**——要有心理预期，
不建议为了过审在名称和描述里伪装功能，违规被发现会封号。

**门槛二：主体。** 如果个人主体走不通，出路是企业主体
（需要营业执照；个体工商户也行）。注册公司/个体户本身
是另一套流程和成本。

**门槛三：审核对内容的敏感性。** 这个工具的完整功能
（POS 终端轮换管理）在审核视角下比单纯记账更敏感。
阶段二提审时建议只带核心功能（卡片周期 + 记账 + 提醒），
机器管理可以后续版本再说——不是隐瞒，而是最小化首审面积，
这在小程序开发里是常规做法。

### 我的建议

网页版已经解决了"能用"的问题。小程序值得做，但**先去把账号注册了、
类目试提一次水**（提审不花钱，驳回不留案底），确认个人主体这条路
通不通，再投入界面重写的工夫。如果你注册好了 AppID 和开发者工具，
下一步我们就把阶段二的小程序做出来。

## 六、里程碑

| 阶段 | 内容 | 状态 |
|---|---|---|
| 1.0 | 网页 PWA + 本地存储 + ICS 提醒 | ✅ 已上线 |
| 2.0 | core.js 抽离 + 测试/CI + 单人模式 | ✅ 完成 |
| 2.1 | 小程序本地版（miniprogram/，复用 core.js） | ✅ 已上传 1.0.3，备案审核中，备案过后提审 |
| 2.2 | 体验打磨：单卡明细页、修复 .ics 转发未发送、提醒只保留「导出 .ics」（写入手机日历功能因安卓限制于 1.0.11 下线，见备忘）、默认商户 1 台「老张便利店」；记账模型改为**消费/还款两本独立流水**（不再逐笔标已还，待还 = 消费总额 − 还款总额，旧数据 migrateRepaid 自动迁移）；自绘还款弹窗（原生 editable 会把 content 预填进输入框）；还款不封顶、超出待还记为**存款**自动抵扣、还清显示「已还清」；设置新增概览排序（紧急/最近变动/自定义↑↓）与记消费选卡平铺/下拉；「最近变动」排序修复：新流水带记账时间戳 ts（旧记录退回按日期），三档排序 = 欠款卡在前 → 操作过的 → 从未记账沉底（core.js：lastActTs/recentBand/recentCmp）；旧占位商户迁移 pruneSeedTerminals（2.1 老设备种下的商户 A–D，没记过账的清掉、清完补「老张便利店」） | ✅ 开发版 **1.0.11** 已上传（cli upload），网页版已上线；待真机验证 + 备案通过后提审 |
| 2.3 | **数据安全专项**：存储读失败不再静默覆盖（两端）、删卡级联删还款、小程序导入走 normalize、孤儿还款自愈、流水排序与筛选修正、提前天数 0 生效、设置页加「本机存储」环境自查；新增 `test/store.test.js`（存储层首次有测试，共 28 项） | ✅ 网页版已上线；小程序开发版 **1.0.14**（1.0.13 加备份文件导出/导入，1.0.14 加版本号显示）。**备份/恢复链路已真机端到端验证通过**（详见下方备忘） |
| 2.4 | **代码审查专项**（结论与修复记录见 `docs/AUDIT_PLAN_20260731.md`）：全库 4 批审查——core.js 领域逻辑、网页版、小程序页面层、工程杂项，**P0/P1 零发现**；修复 4 项 P2：未来周期消费占还款额度改为明示（core 新增 `fut/futN`，两端概览与单卡明细红字提示）、网页版读失败禁止导出空备份（对齐小程序）、SW 只缓存 `res.ok` 响应（错误页不再顶掉离线缓存）、商户间隔与新卡默认提前天数的 0 值生效（`== null` 判空）；后续打磨：设置参数保存加确认反馈（toast 说明存了什么）、清空/乱填不再静默存 0、全局提前天数上限对齐 15（W-d）、空值提示改「已恢复为修改前的 N 天」 | ✅ 网页版已上线；小程序开发版 **1.0.17** 已上传（1.0.15 修 4 项 P2，1.0.16 参数反馈 + W-d，1.0.17 文案打磨），体验版自动跟最新 |
| 2.5 | **外部复审修复（Codex 报，2026-08-07）**：网页版在「本机数据损坏或读不出来」时**整个脚本从 `let S=load()` 就中断**，页面成点不动的空壳，用户进不了「设置 → 粘贴/导入」恢复路径。根因是 TDZ——`loadFailed` 声明在 `let S=load()` 之后，`load()` 的 catch 给它赋值时撞死区抛 ReferenceError（正常路径不进 catch，所以埋了很久没响）。三处修复：`loadFailed` 提前声明（根因）、新增 `firstRun` 供启动种子判断以消灭启动尾部的裸 `localStorage.getItem`（原 W-b）、读失败时启动告知用户「数据没被覆盖、先刷新、别重录、别覆盖旧备份」（对齐小程序端已有的 toast）。复盘与四场景实测记录见 `docs/AUDIT_PLAN_20260731.md`「W-b 复盘」 | ✅ 网页版已上线；小程序端不受影响（`readFailed` 是状态属性不是模块级 let） |
| — | **小程序上架**：ICP 备案通过 → 提审通过 → **正式发布，用户可搜索到** | ✅ **2026-08-07 已上线**。三项待验真机项全部结清（本机存储面板、删卡级联文案、同日多笔排序——最后一项本就有 `core.txnCmp` 与测试固化，非待定项）。AppSecret 轮换经评估**决定不做** |
| 2.6 | **Codex 第二批复审修复**（5 项核实记录见 `docs/AUDIT_PLAN_20260731.md`「Codex 第二批复审」）：金额入口收紧（`core.parseAmount`：有限/正/两位小数/≤1e8，拦 `1e999`→Infinity→JSON 变 null 归零、拦 `0.001`「已登记 ¥0.00」）；导入字段白名单清洗（`core.sanitizeState`，W-a 销案——恶意备份 id 拼进 onclick 的自 XSS 面已封，**新增 settings 字段须同步进白名单**）；小程序 ICS 导出补传 multiUser；清空文案补还款数且 save 成功才报已清空（W-c 销案）；P1「启动尾部裸读」经核实为旧副本残影，当前代码 bb59140 已修 | ✅ 网页版已上线；小程序开发版 **1.0.18** 已上传，测试 36 项全过 |
| 2.7 | **Codex 第三批复审修复**（记录见 `docs/AUDIT_PLAN_20260731.md`「Codex 第三批复审」）：保存卡片做成快照事务——`saveCard` 原本**没有**回滚（批次 3 审查记载与代码不符，已在原文标注更正），save 失败后重试会积累重复卡落盘；`ensureMe` 补的默认持卡人也纳入同一快照（peopleN 必须取在 ensureMe 之前）。小程序下沉为 `store.saveCardData`（可测），网页版 `save()` 改返回布尔、三写路径（saveCard/saveSwipe/doRepay）失败回滚 | ✅ 网页版已上线；小程序开发版 **1.0.19** 已上传，测试 38 项全过 |
| 3.0 | 云开发同步版（共享机器状态 + 订阅消息提醒） | 规划中 |

### 2.1 开发环境备忘

- 开发者工具是 **Electron 新版（2.02+）**，配置目录 `~/Library/Application Support/微信开发者工具/<hash>/`；
  CLI 在 `/Applications/wechatwebdevtools.app/Contents/MacOS/cli`
- 服务端口设置存于 `<hash>/WeappLocalData/localstorage_b72da75d….json`
  （`reduxPersist:settings` → `security.enableServicePort`），CLI 报"服务端口已关闭"时可直接改此文件后重启工具
- `project.config.json` 的 `libVersion` 必须是有效版本或 `latest`——
  无效版本会导致模拟器基础库加载失败与 preview 上传报 `41002 appid missing`（报错信息有误导性）
- 新版工具对 miniprogram-automator 自动化桥暂不响应（命令超时），
  运行时验证以 `cli preview`（编译+上传链路）+ 真机预览为准
- 生成预览二维码：`cli preview --project <miniprogram绝对路径> --qr-format image --qr-output <路径>`（二维码约 25 分钟有效）
- 小程序端修改 core.js 后务必 `npm run sync-core`（CI 的 check-sync 会拦不一致）
- **真机长期测试走体验版，不要用预览码**：预览码 25 分钟过期是微信固定的改不了。
  cli upload 后到 mp.weixin.qq.com → 管理 → 版本管理 → 开发版本 → 「选为体验版」，
  体验版二维码长期有效，扫一次后留在「最近使用」；朋友测试在 成员管理 → 体验成员 里加（上限 90 人）。
  **更正（2026-07-27 真机实测）：首次「选为体验版」之后，后续每次 cli upload 体验版会自动跟到最新，
  进小程序自动刷新，不需要再去后台重选**——本文档此前写的"每次都要手动重选"是错的。
  为了能确认手上跑的到底是哪一版，`utils/store.js` 里硬编码了 `APP_VERSION` 并显示在
  设置页「本机存储」中（`getAccountInfoSync().miniProgram.version` 在开发版/体验版返回空字符串，
  指望不上）。**每次 `cli upload` 改 `-v` 时，记得同步改 `APP_VERSION`。**
- **开发版/体验版/正式版三个环境的 storage 相互独立且换版本不清空**：
  首启种子数据只在存储为空时写入，改种子内容对老设备无效——需要在 normalize 里加迁移
  （先例：pruneSeedTerminals 清理 2.1 的 A–D 占位商户）。
  「扫码进去数据时有时无」的真相通常是**预览码进的开发版、体验码进的体验版，两个空间互不相通**
### 2.3 数据安全备忘（「扫码后数据有时有有时没有」的排查结论）

**用户反馈的现象**：重新扫码进小程序，数据有时在、有时没有。查下来是两件不同的事，别混为一谈：

1. **存储空间隔离（占绝大多数，不是 bug）**——开发版（预览码）/ 体验版（体验码）/ 正式版
   是**三个互不相通的本地存储空间**，扫不同来源的码进去看到的就是不同的数据。
   2.3 起设置页新增「本机存储」板块，直接显示当前 envVersion（开发版/体验版/正式版）、
   本环境的卡与流水条数、占用大小——**下次再出现"数据没了"，先看这里是哪个环境**，
   一眼就能分清是走错空间还是真丢了。跨空间搬数据用「复制备份 / 从剪贴板导入」。

2. **`store.load()` 的静默覆盖（真 bug，已修）**——老代码 `try{ getStorageSync }catch(e){}`
   把读取异常吞掉后 `d` 为 null，于是**当成首次启动、立刻写入空种子，把盘上的真数据覆盖掉**，
   全程无提示且不可逆。修法（`utils/store.js`）：
   - `readRaw()` 区分「读到空」与「读失败」，失败重试一次；
   - 读失败返回带 `readFailed` 标记的空状态，**不写盘、不种种子**，弹提示让用户重启；
   - `save()` 拒绝写回任何带 `readFailed` 的状态（防御第二层），返回 false；
   - 上层（记消费 / 登记还款 / 保存卡片）拿到 false 就回滚内存并如实报错，
     不再出现「提示已保存、其实没写进去」；`exportJSON` 在读失败时拒绝导出空备份。
   - 网页版同一根因同样修了：`load()` 解析失败置 `loadFailed`，`save()` 一律拦截；
     **但导入和清空是用户明确的覆盖动作，会解除拦截**——这是读失败后的恢复路径，别堵死。

**备份/恢复真机验证结论（2026-07-27，三份实测备份逐份校验）**：
小程序的「导出备份文件 → 转发到聊天 → 从文件导入」整条链路端到端跑通，
`shareFileMessage` / `chooseMessageFile` 在真机可用。三份备份递进验证：

| 时间 | 消费 | 还款 | 净待还 | 验到了什么 |
|---|---|---|---|---|
| 21:53 | 0 笔 | 0 笔 | — | 文件格式、编码、往返无损（但无流水，等于没验到关键项） |
| 22:06 | 2 笔 ¥45,000 | 0 笔 | ¥45,000 | **消费流水确实进备份**，含记账时刻 ts |
| 22:08 | 2 笔 ¥45,000 | 2 笔 ¥27,000 | ¥18,000 | **还款流水确实进备份**，逐卡待还导入前后完全一致 |

逐卡对账：5 号卡消费 3 万还 2 万 → 待还 1 万；15 号卡消费 1.5 万还 0.7 万 → 待还 0.8 万；
导入后两张卡待还分毫不差，还款各自挂在正确的卡上没串卡。
**教训：验备份别拿空数据验**——第一份 18 项全过但流水是 0，看着漂亮其实什么都没证明。
往返无损已固化为 `test/store.test.js` 里的测试（用 6 笔消费 + 6 笔还款，断言恢复后待还金额一致）。

**其余同批修复**（都有测试）：删卡/删持卡人必须连还款一起删（`core.removeCard/removePerson`），
老数据里的孤儿还款在 `normalize` 里由 `core.pruneOrphans` 自愈——
**`pruneOrphans` 有个安全阀：cards 为空时原样返回**，因为「没有卡」既可能是真没有，
也可能是读取异常拿到空壳，后者若按孤儿清理会把全部流水删光。

- **安卓 wx.addPhoneCalendar 批量写入：机制上不可行，已放弃**（三轮真机排查的完整证据链）：
  ① 串行回调链/setTimeout 从第二条起报 `can only be invoked by user TAP gesture`（1.0.5/1.0.7）；
  ② 点击链里同步并发 84 条（1.0.8）：**每条都要用户手动点一次「创建日历」系统确认框**，
  并发时只弹前 1–2 个框，**其余 82 条没写进去也照样回调 success**（回调计数完全不可信）。
  即批量写 = 让用户点 84 次确认，且点了也未必真写。
  **1.0.11 起「写入手机日历」功能整个下线（用户拍板：别搞来搞去），提醒只保留「导出 .ics」**——
  一次导入全部、还款自带 3天前/1天前/当天 三响、全平台一致。将来别再试图恢复批量写日历
